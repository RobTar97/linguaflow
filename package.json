{
  "name": "linguaflow",
  "private": true,
  "version": "0.3.1",
  "description": "Open-source English, Polish, and Japanese conversation practice for learners and teachers.",
  "license": "MIT",
  "keywords": [
    "language-learning",
    "conversation-practice",
    "education",
    "cloudflare-workers",
    "react"
  ],
  "engines": {
    "node": ">=22.13.0",
    "npm": ">=10"
  },
  "overrides": {
    "sharp": "0.35.3"
  },
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "npm run preview:cloudflare",
    "preview:static": "vite preview",
    "preview:cloudflare": "npm run build && wrangler dev",
    "deploy": "npm run check && wrangler deploy",
    "types:worker": "tsc -p tsconfig.worker.json",
    "lint": "eslint .",
    "test": "vitest run --maxWorkers=1",
    "audit:secrets": "node scripts/check-secrets.mjs",
    "audit:performance": "node scripts/check-performance-budget.mjs",
    "check": "npm run audit:secrets && npm run lint && npm run test && npm run build && npm run audit:performance && npm run types:worker",
    "check:content": "vitest run --maxWorkers=1 src/catalog/topicCatalog.test.ts"
  },
  "dependencies": {
    "framer-motion": "^12.23.12",
    "lucide-react": "^0.468.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260729.1",
    "@eslint/js": "^10.0.1",
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7",
    "@vitejs/plugin-react": "^5.0.2",
    "eslint": "^10.8.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.3",
    "globals": "^16.3.0",
    "typescript": "~5.8.3",
    "typescript-eslint": "^8.65.0",
    "vite": "^7.1.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.115.0"
  }
}
