{
  "name": "linguaflow",
  "private": true,
  "version": "0.2.0",
  "description": "Open-source English, Polish, and Japanese conversation practice for learners and teachers.",
  "license": "MIT",
  "keywords": [
    "language-learning",
    "conversation-practice",
    "education",
    "cloudflare-workers",
    "react"
  ],
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  },
  "overrides": {
    "sharp": "0.35.3"
  },
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "npm run preview:cloudflare",
    "preview:static": "vite preview",
    "preview:cloudflare": "npm run build && wrangler dev",
    "deploy": "npm run check && wrangler deploy",
    "types:worker": "tsc -p tsconfig.worker.json",
    "lint": "eslint .",
    "test": "vitest run --maxWorkers=1",
    "audit:secrets": "node scripts/check-secrets.mjs",
    "check": "npm run audit:secrets && npm run lint && npm run test && npm run build && npm run types:worker",
    "check:content": "vitest run --maxWorkers=1 src/catalog/topicCatalog.test.ts"
  },
  "dependencies": {
    "framer-motion": "^12.23.12",
    "lucide-react": "^0.468.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260719.1",
    "@eslint/js": "^10.0.1",
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7",
    "@vitejs/plugin-react": "^6.0.4",
    "eslint": "^10.7.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.3",
    "globals": "^17.7.0",
    "typescript": "~7.0.2",
    "typescript-eslint": "^8.65.0",
    "vite": "^8.1.5",
    "vitest": "^4.1.10",
    "wrangler": "^4.113.0"
  }
}
