# Approved bundled packs

Maintainers may place reviewed `.lfpack` archives in this directory. The build
discovers them automatically, runs the same browser parser at startup, and adds
valid topics to the runtime catalog. Each archive still carries its own license,
authorship, compatibility, source, and review assertions.
